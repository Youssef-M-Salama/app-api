namespace App.Core.Enums
{
    /// <summary>
    /// Represents the type of account being registered.
    /// </summary>
    public enum AccountType
    {
        /// <summary>A charity organization that posts needs and applies to offers.</summary>
        Charity,

        /// <summary>A donor organization that posts offers and applies to needs.</summary>
        DonorOrganization
    }
}